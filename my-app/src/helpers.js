const help = require("./Dashboard");
describe('dashboard module', () => {
    describe('componentDidUpdate=>reset balls/strikes when strikes=0 or balls=4', () => {
        it('when strikes=0 or balls=4', () => {
            const expectedOutput = 
        })
    })
})